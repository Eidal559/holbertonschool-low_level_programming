#include "4-sum.h"
#include <stdio.h>

/**
* main - Entry point
*
* Description: Uses the SUM macro to compute and print
* the sum of 98 and 1024.
*
* Return: Always 0 (Success)
*/

int main(void)
{
int s;

s = SUM(98, 1024);
printf("%d\n", s);
return (0);
}

#include <stdio.h>

int main(void)
{
for (char letter = 'a'; letter <= 'z'; letter++)
{
putchar(letter);
}
putchar('\n');
for (char letter = 'A'; letter <= 'Z'; letter++)
{
putchar(letter);
}
putchar('\n');
return (0);
}
